<div class="cards">
  {{#each artistAlbums}}
  <h1>Albums for: {{this.name}}</h1>
  <div class="card">
    <img src="{{this.images.[0].url}}" />
    <a href="/tracks/{{this.id}}">View Tracks</a>
  </div>
  {{/each}}
</div>